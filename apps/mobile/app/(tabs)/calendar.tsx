import { Text, View } from 'react-native';

export default function Calendar() {
  return (
    <View className="flex-1 items-center justify-center bg-kairo-bg">
      <Text className="font-sans text-kairo-muted">Calendar — coming in v1.1</Text>
    </View>
  );
}
